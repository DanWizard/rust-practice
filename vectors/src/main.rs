fn main() {
    // let vector: Vec<u16> = Vec::new();
    let mut vector = vec![1, 2, 3, 4];

    vector.push(5);

    // println!("{}", );
    for i in vector.iter() {
        println!("{}", i);
    }
}
